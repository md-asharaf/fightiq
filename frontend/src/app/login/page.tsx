import { LoginView } from "@/components/auth/LoginView";

export const metadata = {
  title: "Login | FightIQ",
  description: "Sign in to your FightIQ account.",
};

export default function LoginPage() {
  return <LoginView />;
}
